{
	"name": "lighthouse-plugin-something",
	"version": "1.0.0",
	"description": "A plugin for Google's Lighthouse",
	"main": "plugin.js",
	"scripts": {
		"build": "tsc",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"keywords": [
		"lighthouse"
	],
	"author": "Torre Taylor",
	"license": "ISC",
	"peerDependencies": {
		"lighthouse": "^5.6.0"
	},
	"devDependencies": {
		"lighthouse": "^5.6.0"
	}
}
